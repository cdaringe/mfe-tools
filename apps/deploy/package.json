{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "parcel serve index.html",
    "build": "parcel build index.html"
  },
  "dependencies": {
    "ui": "workspace:*",
    "mfe-core": "workspace:*"
  },
  "devDependencies": {
    "config": "workspace:*",
    "tsconfig": "workspace:*",
    "mfe-admin-gql-client": "workspace:*"
  }
}
